@model List<MilkWayIndia.Models.SubcatViewModel>

@if (Model != null)
{

    <input id="myCheckbox" type="checkbox" /><label>&nbsp; Select All</label>
    foreach (var item in Model)
    {
        <div>
            @*@Html.HiddenFor(m => item.ID)
                @Html.HiddenFor(m => item.Name)*@
            <input type="checkbox" name="chkSubcat[]" value="@item.ID" @item.IsSelected />
            @item.Name
        </div>
    }
}
<script>
    $('#myCheckbox').click(function () {
        if ($(this).is(":checked")) {
            $('#div_subcat').find('input[type="checkbox"]').each(function () {
                $(this).prop("checked", true);
            });
        }
        else {
            $('[id *= div_subcat]').find('input[type="checkbox"]').each(function () {
                $(this).prop("checked", false);
            });
        }
    });
</script>
